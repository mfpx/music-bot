aiohttp
aiosqlite
discord.py
pynacl
# pypi doesn't have the latest version, so we must install it from github using the master branch
#git+https://github.com/ytdl-org/youtube-dl.git@master #broken
git+https://github.com/yt-dlp/yt-dlp.git@master # this is a fork, seems to work better for now